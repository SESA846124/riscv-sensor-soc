name: Hello World

on:
  push :
    branches:
      - main
       
jobs:
  name: greetings
  runs on: [self-hosted,linux]

    steps:
      - name: Greet
        run: echo "Hello World! This is my first GitHub Action workflow."

