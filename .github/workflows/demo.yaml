name: Hello World

on:
 workflow_dispatch:
   inputs:
     name:
       description: 'Your name'
       required: true
       
jobs:
  name: greetings
    steps:
      - name: Greet
        run: echo "Hello world!"

      - name: Greet again
        run: echo "Hello again!"
